# build artifacts (main.js is attached to GitHub releases, not committed)
node_modules/
/main.js
*.map

# runtime / local state that must never be published
/data.json
*.bak
.hotreload
.claude/
.cache/
*.log
.DS_Store
